const reuse= require('./util/reuse');

reuse.addNumber(1,2);
reuse.getSizeOfArray([1,2,3,4,5]);
reuse.printArray([1,2,3,4,5]);
reuse.printJSON(
    {
        "key1":"Hi",
        "key2":"Isha"
    }
)